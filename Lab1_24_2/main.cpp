#include <iostream>      // Librería para entrada y salida estándar
#include <iomanip>       // Librería para manipulación de formato de salida
#include "Funciones.h"   // Inclusión del archivo de cabecera con las funciones declaradas

using namespace std;     // Usar el espacio de nombres estándar para evitar escribir std::

int main() {
    // Llama a la función principal que imprime el reporte completo
    ImprimirReporte();
}